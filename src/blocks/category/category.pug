mixin b-category(imgUrl, title, subtitle)
	+b("a").category(href="#")
		+e("figure").img-wrap
			+e("img").img(src=asset(imgUrl))

		+e.content(class=classes)
			+e.title= title
			+e.subtitle= subtitle
