mixin b-nav(links, activeIndex)
	+b('nav').nav
		each link, index in links
			- var classes = ''
			if index == activeIndex
				- classes = 'active'
			+e('a').link(href=link.url class=classes)= link.title